const { expect } = require('chai')
const twoSum = require('./twoSum')

describe('the twoSum function', () => {
    it('is a function', () => {
        expect(twoSum).to.be.a('function');
    });

    it('takes an array of numbers and a number', () => {
        expect(twoSum([1,2,3], 5)).
    })
})